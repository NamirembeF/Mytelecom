<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.first2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.First2">
        <activity android:name=".Navigation"></activity>
        <activity android:name=".ImageSlider" />
        <activity android:name=".Slider_Main" />
        <activity android:name=".MainActivity" />
        <activity android:name=".BuyActivity" />
        <activity android:name=".CountrySelection" />
        <activity android:name=".Services">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>